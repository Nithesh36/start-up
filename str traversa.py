#Traversal and the for Loop: By Index¶
fruit = "apple"
for idx in range(len(fruit)):
    print(fruit[idx])#we need a temporary variable to print the  specified index value in the string
    #here  we doesnot use so it will print all the values one  by one or iterate


fruit = "apple"
re=int(input("enter a range\n"))
for idx in range(re):#suppose the value is 2
#output is a,p ..here the val is index position fruit[0]
#fruit[1] can be printed  ...(a,p)is the result
   print(fruit[idx])

#here we can print a value in line by line based on given range



#output 
a
p